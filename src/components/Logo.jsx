import React from 'react';

export default function Logo({ className }) {
  return (
    <div className={`w-8 h-8 rounded-xl bg-blue-600 text-white flex items-center justify-center font-bold text-sm select-none shrink-0 ${className}`}>
      Ae
    </div>
  );
}
